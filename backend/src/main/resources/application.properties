# Local MySQL database
spring.datasource.url=jdbc:mysql://localhost:3306/icebreaker_db?useSSL=false
spring.datasource.username=admin
spring.datasource.password=P@ssw0rd!123

# Spring JPA/Hibernate properties
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update

# Load data from data.sql
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always
spring.jpa.properties.hibernate.hbm2ddl.import_files=data.sql

# Server Properties
server.port=8080

# App Properties
icebreaker.app.jwtSecret= ======================JWTIcebreakerSecret===========================
icebreaker.app.jwtExpirationMs=86400000